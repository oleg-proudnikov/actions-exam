name: workflow1
on:
  workflow_dispatch:

  workflow_call:

jobs:
  deploy:
    name: Import external glossaries
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v3

      ##############################
      - name: Import external glossaries
        run: echo workflow1 > workflow1.txt
      ##############################



      ##############################
      - name: Commit and push the changes
        run: |
          cat .git/config

          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          #git remote set-url origin https://${{ secrets.GITHUB_TOKEN }}@github.com/oleg-proudnikov/actions-exam.git

          git add .
          git commit -m "chore: fetch content from various external sources"
          git pull
          git push
          git status
